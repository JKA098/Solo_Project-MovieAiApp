# Solo_Project-MovieAiApp
PopChoice AI is a movie recommendation app powered by OpenAI embeddings, Supabase vector search, and GPT. Users enter preferences, and the app retrieves similar movies using semantic search and generates personalized suggestions through a lightweight RAG pipeline in vanilla JavaScript.
